<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">
    <activeProfiles>
        <activeProfile>github</activeProfile>
    </activeProfiles>
    <profiles>
        <profile>
            <id>github</id>
            <repositories>
                <repository>
                    <id>central</id>
                    <url>https://repo1.maven.org/maven2</url>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>
                </repository>
                <repository>
                    <id>vanillabp-spi-for-java</id>
                    <url>https://maven.pkg.github.com/vanillabp/spi-for-java</url>
                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>
                    <releases>
                        <enabled>false</enabled>
                    </releases>
                </repository>
                <repository>
                    <!--
                      Camunda 7 enterprise artifacts. Since adapter 1.6.0 this build needs them: Spring
                      Boot 4 support arrived in Camunda 7.24.3 and the 7.24 line is enterprise-only.

                      The id matches the repository declared in the pom, and a repository defined in an
                      active settings profile wins over a pom repository of the same id. So the pom keeps
                      pointing at the internal Nexus, which is what developers reach through the company
                      VPN, while the build here resolves the very same artifacts straight from Camunda -
                      GitHub runners have no VPN. Credentials come from the CAMUNDA_EE_USR / CAMUNDA_EE_PSW
                      secrets, see the server entry below.
                    -->
                    <id>camunda-enterprise</id>
                    <url>https://artifacts.camunda.com/artifactory/camunda-bpm/</url>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>
                </repository>
                <repository>
                    <id>vanillabp-spring-boot-support</id>
                    <url>https://maven.pkg.github.com/vanillabp/spring-boot-support</url>
                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>
                    <releases>
                        <enabled>false</enabled>
                  </releases>
                </repository>
            </repositories>
        </profile>
    </profiles>

    <servers>
        <server>
            <id>vanillabp-spring-boot-support</id>
            <username>${env.USER_NAME}</username>
            <password>${env.USER_TOKEN}</password>
        </server>
        <server>
            <id>vanillabp-spi-for-java</id>
            <username>${env.USER_NAME}</username>
            <password>${env.USER_TOKEN}</password>
        </server>
        <server>
            <id>camunda-enterprise</id>
            <username>${env.CAMUNDA_EE_USR}</username>
            <password>${env.CAMUNDA_EE_PSW}</password>
        </server>
        <server>
            <id>github</id>
            <username>${env.USER_NAME}</username>
            <password>${env.CAMUNDA_USER_TOKEN}</password>
        </server>
    </servers>

</settings>

